import { CurrencySuffixPipe } from './currency-suffix.pipe';

describe('CurrencySuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencySuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
